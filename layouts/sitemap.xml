{{ printf "<?xml version=\"1.0\" encoding=\"utf-8\" standalone=\"yes\"?>" | safeHTML }}
<urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9"
  xmlns:xhtml="http://www.w3.org/1999/xhtml">
{{- range .Data.Pages }}
  {{- $isEn := eq .Language.Lang "en" }}
  {{- $selfHasFile := and .File (ne .File.Path "") }}
  {{- if and $isEn (not $selfHasFile) }}{{/* English: skip pages without an actual content file */}}
  {{- else if .Permalink }}
  {{- $validTranslations := slice }}
  {{- range .Translations }}
    {{- if and .File (ne .File.Path "") }}
      {{- $validTranslations = $validTranslations | append . }}
    {{- end }}
  {{- end }}
  <url>
    <loc>{{ .Permalink }}</loc>
    {{- if not .Lastmod.IsZero }}
    <lastmod>{{ .Lastmod.Format "2006-01-02T15:04:05-07:00" | safeHTML }}</lastmod>
    {{- end }}
    {{- if and $selfHasFile $validTranslations }}
    <xhtml:link rel="alternate" hreflang="{{ .Language.Lang }}" href="{{ .Permalink }}" />
      {{- range $validTranslations }}
    <xhtml:link rel="alternate" hreflang="{{ .Language.Lang }}" href="{{ .Permalink }}" />
      {{- end }}
    {{- end }}
  </url>
  {{- end }}
{{- end }}
</urlset>
